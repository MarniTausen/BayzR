#' Fixed variable
#'
#' Sets a variable to a fixed variable in a linear mixed model.
#'
#' @param x   Variable vector which should be fixed in a model.
#'
#' @return Factorized version of the vector
#' @export

fixf <- function(x) as.factor(x)
